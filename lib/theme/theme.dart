import 'package:flutter/material.dart';
final suLightTheme = ThemeData(
  useMaterial3: true,
  colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF005BBB)),
);

/// App theme (colors, typography).
/// Frontend-only.
/// Future: add dark theme + custom text styles.