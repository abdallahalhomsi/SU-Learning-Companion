// flashcards_screen.dart
import 'package:flutter/material.dart';
class FlashcardsScreen extends StatelessWidget { const FlashcardsScreen({super.key});
@override Widget build(BuildContext c)=>const Scaffold(body: Center(child: Text('Flashcards'))); }


/// Flashcards UI stub.
/// Future: deck list, create cards, study flow.