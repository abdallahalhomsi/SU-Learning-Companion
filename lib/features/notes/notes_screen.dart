import 'package:flutter/material.dart';
class NotesScreen extends StatelessWidget { const NotesScreen({super.key});
@override Widget build(BuildContext c)=>const Scaffold(body: Center(child: Text('Notes'))); }

/// Notes UI stub.
/// Future: per-course notes, markdown/plain text, links.